# Required by the server when configs/config.yaml has no API keys.
JWT_SECRET=replace-with-at-least-32-random-characters

# Optional overrides; see README.md for the complete configuration reference.
CONFIG_PATH=configs/config.yaml
